// Operators



1. AND (both condition must be true)
condition1 && condition2
// true && true

2. OR (any one must be true)
condition1 || condition2

3. not (opposite value)

const isAdult = true;
console.log(!isAdult);

const isAdult = false;

if (!isAdult) {
    console.log()
}